# 运行指南

> AI 智能客服系统 — 完整运行说明

---

## 目录

- [系统架构概览](#系统架构概览)
- [前置环境要求](#前置环境要求)
- [快速启动（完整流程）](#快速启动完整流程)
- [详细配置说明](#详细配置说明)
- [启动后端服务](#启动后端服务)
- [启动前端服务](#启动前端服务)
- [初始化知识库](#初始化知识库)
- [访问系统](#访问系统)
- [运维与调试](#运维与调试)
- [常见问题排查](#常见问题排查)

---

## 系统架构概览

```text
┌─────────────┐     ┌───────────────┐     ┌──────────────────┐
│  前端 SPA   │────▶│   FastAPI     │────▶│   MySQL 8.0+     │
│ (React 19)  │     │  (Port 7500)  │     │  (Port 7334)     │
│ (Port 5173) │     │               │────▶│   Qdrant         │
│             │     │               │     │  (Port 7333)     │
│ Vite Proxy  │     │               │────▶│   LLM API        │
│ /api → :7500│     │               │     │  (DeepSeek /      │
└─────────────┘     │               │     │   OpenAI 兼容)   │
                    │               │────▶│   Embedding API  │
                    │               │     │  (SiliconFlow)   │
                    └───────────────┘     └──────────────────┘
```

**外部依赖**：

| 服务 | 端口（示例） | 用途 |
|------|-------------|------|
| MySQL | 7334 | 业务数据存储（用户、会话、文档元数据、对话状态） |
| Qdrant | 7333 | 向量存储与混合检索 |
| LLM API | — | 对话生成（OpenAI 兼容接口） |
| Embedding API | — | 文本向量化（OpenAI 兼容接口） |

---

## 前置环境要求

### 系统要求

- **操作系统**：Linux / macOS / Windows (WSL2)
- **Python** >= 3.11
- **Node.js** >= 18
- **pnpm** >= 8（`npm install -g pnpm`）
- **uv** >= 0.4（Python 包管理器，参考 [uv 官方文档](https://docs.astral.sh/uv/)安装）
- **Docker**（可选，用于快速启动 MySQL 和 Qdrant）

### 端口要求

确保以下端口未被占用：

| 端口 | 用途 |
|------|------|
| 7500 | 后端 FastAPI 服务 |
| 5173 | 前端 Vite 开发服务器 |
| 7333 | Qdrant 向量数据库 |
| 7334 | MySQL 数据库 |

> 端口可在 `.env` 文件中修改。

---

## 快速启动（完整流程）

```bash
# 1. 启动 MySQL 和 Qdrant（使用 Docker）
docker run -d --name mysql-rag \
  -e MYSQL_ROOT_PASSWORD=123456 \
  -e MYSQL_DATABASE=simplerag \
  -p 7334:3306 \
  mysql:8.0

docker run -d --name qdrant-rag \
  -p 7333:6333 \
  qdrant/qdrant

# 2. 配置后端
cd backend
cp .env.example .env
# 编辑 .env，填入 API Key（详见下方配置说明）

# 3. 安装后端依赖
uv sync

# 4. 启动后端（终端 1）
uv run uvicorn main:app --reload --host 0.0.0.0 --port 7500

# 5. 安装前端依赖（新终端）
cd frontend
pnpm install

# 6. 启动前端（终端 2）
pnpm dev

# 7. 初始化知识库
cd backend
uv run python -m app.scripts.init_kb

# 8. 访问系统
# 浏览器打开 http://localhost:5173
```

---

## 详细配置说明

### 配置方式

所有配置通过 `backend/.env` 文件管理，由 `pydantic-settings` 自动加载。

### 配置项一览

#### MySQL 配置

```ini
DB_HOST=127.0.0.1
DB_PORT=7334
DB_USER=root
DB_PASSWORD=123456
DB_NAME=simplerag
```

> 需提前创建好数据库（`CREATE DATABASE simplerag`）。表结构在首次启动时自动创建。

#### Qdrant 配置

```ini
QDRANT_HOST=127.0.0.1
QDRANT_PORT=7333
QDRANT_API_KEY=your_api_key_here
QDRANT_COLLECTION_NAME=simplerag
```

> Qdrant 默认不启用 API Key，可留空。Collection 在首次写入数据时自动创建。

#### 对话模型配置（OpenAI 兼容）

```ini
OPENAI_BASE_URL=https://api.qnaigc.com/v1
OPENAI_MODEL=deepseek/deepseek-v4-flash
OPENAI_API_KEY=sk-xxxx
```

**支持的模型示例**：

| 模型 | 提供方 | 备注 |
|------|--------|------|
| deepseek/deepseek-v4-flash | DeepSeek | 性价比高，推荐 |
| gpt-4o-mini | OpenAI | 效果稳定 |
| gpt-4o | OpenAI | 效果最好但较贵 |
| qwen-turbo | 通义千问 | 免费额度可用 |
| 本地 Ollama 模型 | Ollama | 需本地 GPU |

> 任何兼容 OpenAI API 的模型都可以使用，仅需修改 `OPENAI_BASE_URL`、`OPENAI_MODEL`、`OPENAI_API_KEY`。

#### 嵌入模型配置（独立配置）

```ini
EMBEDDING_API_BASE_URL=https://api.siliconflow.cn/v1
EMBEDDING_API_KEY=sk-xxxx
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B
EMBEDDING_VECTOR_SIZE=4096
```

**嵌入模型选择建议**：

| 模型 | 维度 | 提供方 | 备注 |
|------|------|--------|------|
| Qwen/Qwen3-Embedding-8B | 4096 | SiliconFlow | 中文强，免费 |
| text-embedding-3-small | 1536 | OpenAI | 通用稳定 |
| text-embedding-3-large | 3072 | OpenAI | 效果更好，较贵 |
| bge-m3 | 1024 | BAAI | 可本地部署 |

> **重要**：`EMBEDDING_VECTOR_SIZE` 必须与嵌入模型的实际输出维度严格一致，否则 Qdrant 查询会报维度不匹配错误。

#### JWT 配置

```ini
JWT_SECRET_KEY=change-me-to-a-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7
```

> 生产环境请务必修改 `JWT_SECRET_KEY` 为一个随机字符串。

#### 其他配置

```ini
# 知识库文档根目录（为空时自动取项目根目录下的 knowledges/）
KNOWLEDGE_BASE_DIR=

# 每用户每日对话次数上限
CHAT_DAILY_LIMIT=100
```

### 完整 .env 示例

```ini
# MySQL
DB_HOST=127.0.0.1
DB_PORT=7334
DB_USER=root
DB_PASSWORD=123456
DB_NAME=simplerag

# Qdrant
QDRANT_HOST=127.0.0.1
QDRANT_PORT=7333
QDRANT_API_KEY=
QDRANT_COLLECTION_NAME=simplerag

# LLM (DeepSeek)
OPENAI_BASE_URL=https://api.qnaigc.com/v1
OPENAI_MODEL=deepseek/deepseek-v4-flash
OPENAI_API_KEY=sk-your-key-here

# Embedding (SiliconFlow)
EMBEDDING_API_BASE_URL=https://api.siliconflow.cn/v1
EMBEDDING_API_KEY=sk-your-key-here
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B
EMBEDDING_VECTOR_SIZE=4096

# JWT
JWT_SECRET_KEY=your-random-secret-key-here

# App
APP_HOST=0.0.0.0
APP_PORT=7500
CHAT_DAILY_LIMIT=100
```

---

## 启动后端服务

### 安装依赖

```bash
cd backend
uv sync
```

`uv sync` 会根据 `pyproject.toml` 自动创建虚拟环境并安装所有依赖。

### 启动命令

```bash
# 开发模式（热重载，推荐）
uv run uvicorn main:app --reload --host 0.0.0.0 --port 7500

# 或直接运行
uv run python main.py

# 生产模式（多 worker）
uv run uvicorn main:app --host 0.0.0.0 --port 7500 --workers 4
```

### 验证启动

```bash
# 健康检查
curl http://localhost:7500/docs

# Swagger 文档
# 浏览器打开 http://localhost:7500/docs
```

启动日志应包含：

```
INFO:     Started server process [xxx]
INFO:     Waiting for application startup.
INFO:     数据库表创建完成
INFO:     应用启动完成
INFO:     Uvicorn running on http://0.0.0.0:7500
```

---

## 启动前端服务

```bash
cd frontend
pnpm install
pnpm dev
```

启动后：

- 开发服务器运行在 `http://localhost:5173`
- Vite 自动将 `/api/*` 请求代理到 `http://127.0.0.1:7500`（自动剥离 `/api` 前缀）
- 无需配置 CORS

### 生产构建

```bash
pnpm build
```

构建产物输出到 `frontend/dist/`，可部署到任意静态文件服务器。

---

## 初始化知识库

系统启动后需要初始化知识库数据：

```bash
cd backend
uv run python -m app.scripts.init_kb
```

该脚本会：

1. 扫描 `knowledges/<kb_name>/` 目录下的所有 `.md` / `.txt` 文件
2. 计算每个文件的 MD5 哈希
3. 跳过已有哈希值的文件（幂等执行）
4. 解析 → 分块 → 向量化 → 写入 Qdrant

**预期输出**：

```
INFO - 开始扫描知识库目录...
INFO - 创建知识库记录: auperator
INFO - 解析文档: knowledges/auperator/auperator-intro.md
INFO - 解析文档: knowledges/auperator/faq.md
INFO - 解析文档: knowledges/auperator/return-policy.txt
INFO - 已写入 5/12 个向量
INFO - 已写入 10/12 个向量
INFO - 写入完成: 共 12 个向量到 Qdrant
INFO - 知识库初始化完成
```

### 其他脚本

```bash
# 交互式检索测试
uv run python -m app.scripts.test_retrieval

# RAG 评估流水线
uv run python -m app.scripts.rag_eval
```

---

## 访问系统

启动完成后，浏览器打开 `http://localhost:5173`：

### 使用流程

1. **注册账号**：输入邮箱和密码
2. **登录系统**：使用注册的邮箱和密码登录
3. **开始对话**：在聊天页面输入问题（如"什么是 Auperator 智能运维系统？"）
4. **查看回答**：AI 的回答会流式逐字显示
5. **评价回答**：对 AI 的回答进行点赞或点踩
6. **管理知识库**：在知识库页面查看/上传/删除文档
7. **历史会话**：在左侧边栏查看和切换历史会话

---

## 运维与调试

### 查看日志

```bash
# 后端日志（控制台直接输出）
uv run uvicorn main:app --reload --host 0.0.0.0 --port 7500 2>&1 | tee backend.log

# 日志文件位置
ls -la backend/logs/
```

### 数据库管理

```bash
# 连接 MySQL
mysql -h 127.0.0.1 -P 7334 -u root -p simplerag

# 查看 Qdrant 状态
curl http://localhost:7333/collections
```

### 常用调试命令

```bash
# 检查路由注册情况
uv run python -c "import main; [print(r.path, r.methods) for r in main.app.routes]"

# 代码检查
cd backend && uv run ruff check app/

# 格式化检查
cd backend && uv run ruff format --check app/

# 前端检查
cd frontend && pnpm lint
```

---

## 常见问题排查

### 1. 启动报错 "ModuleNotFoundError"

```bash
# 确认已安装依赖
cd backend && uv sync
```

### 2. 数据库连接失败

```
ERROR: Can't connect to MySQL server
```

**排查步骤**：

1. 确认 MySQL 容器在运行：`docker ps | grep mysql`
2. 确认端口映射正确：`docker logs mysql-rag`
3. 尝试连接：`mysql -h 127.0.0.1 -P 7334 -u root -p`

### 3. Qdrant 连接失败

```
ERROR: Failed to connect to Qdrant at 127.0.0.1:7333
```

**排查步骤**：

1. 确认 Qdrant 容器在运行：`docker ps | grep qdrant`
2. 检查状态：`curl http://localhost:7333/health`

### 4. LLM API 返回错误

```
ERROR: OpenAI API error: 401 Unauthorized
```

- 检查 `.env` 中的 `OPENAI_API_KEY` 是否正确
- 确认 API Key 有足够的余额/额度

### 5. Embedding API 返回错误

```
ERROR: Embedding API error: 401 Unauthorized
```

- 检查 `.env` 中的 `EMBEDDING_API_KEY` 是否正确
- 确认 `EMBEDDING_VECTOR_SIZE` 与模型输出维度一致

### 6. 前端请求报 401

- Token 过期：重新登录
- Token 未携带：确认 localStorage 中有 `access_token`

### 7. 知识库初始化后，提问仍提示"无法回答"

- 确认 init_kb 脚本执行成功且输出 "写入完成"
- 检查知识库文档的 status 是否为 "ready"
- 尝试在系统知识库页面查看文档列表确认状态

### 8. 前端页面白屏或路由异常

```bash
# 清理缓存后刷新
# 或检查浏览器控制台报错

# 重新构建前端
cd frontend && pnpm build
```
